// JavaScript to add class on load
window.addEventListener("load", function () {
    const contentContainer = document.querySelector('.content-container');
    contentContainer.classList.add('animate'); // Add a class to trigger animation
});
